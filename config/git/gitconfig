#
#
#

[alias]
	hist = log --pretty=format:\"%C(auto)%h%C(reset) %C(blue)%C(italic)%ad%C(reset) | %s%C(auto)%d%C(reset) [%C(green)%an]\" \
				 	 --graph --date=short
	unstage = restore --staged
	last = log -1 HEAD
